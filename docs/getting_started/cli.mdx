# CLI Tool (Beta)

To create a new project, run the following command:

```bash
npx create-rhino-app@beta
```

Not working for you or need more control? Check out the [docker](docker) or [manual](manual) setup instructions.
